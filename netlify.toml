[build]
command = "npm run build"
publish = "build"

[build.environment]
NODE_VERSION = "20"

# SPA fallback — client-side routing (adapter-static fallback: index.html)
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
